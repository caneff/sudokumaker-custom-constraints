# QQRR: the 7-digit seeing tie exists once the 33 cage moves to r5c1

**Question (Chris, 2026-09-22, after #601):** drop the entered digits and the
uncircled marks; keep the QR 10 (window top-left r6c6), the bands 51..56 (r2c5)
and 58+ (r4c4), and a QQRR 5 in a corner; move the QQRR 33 cage from r1c5 to
r5c1. Does a completion now hold two interior cells that see each other with
equal 7-digit cell numbers from different rank lists?

**Runs.** `finders/qqrr/qqrr_cpsat.py <variant opener> --corner <c> --tie
--count 2 --workers 6 --timeout 1800` on `main` at 4347b12, the variant being
`opener.json` with the cage's cell index 4 replaced by 36 (r5c1), four corners
in parallel. Entered digits and marks are solver hints only (`--hypotheses`
off).

| corner | verdict |
|---|---|
| br | **multiple**: 2 found in 571.9 s (count capped at 2) |
| tl, tr, bl | stopped before a verdict when Chris changed the ask (below) |

**The two bottom-right grids** (both re-ranked by `oracle.py`; clue values in
parentheses are the grid's actual ranks inside the bands):

1. `591247638/672183954/438695127/865934271/927516483/143872569/284369715/716458392/359721846`
   — QR 10, 61, 53; QQRR 33 at r5c1, 5 at r9c9. Tie **r7c2 = r7c8**, same row:
   `3|24|14|54` and `32|41|45|4`, both 3241454, both QQRR 43.
2. `781235469/692184735/534769281/825973614/413856972/976412358/169527843/257348196/348691527`
   — QR 10, 64, 56; QQRR 33 at r5c1, 5 at r9c9. Tie **r5c2 = r8c2**, same column
   and r5c2 shares a window with the 33 cage: `54|11|23|3` and `5|41|12|33`,
   both 5411233, both QQRR 46.

Both are presets at the top of `docs/research/2026-09-22-qqrr-explorer.html`,
which now draws the cage where the preset says.

## Second ask: drop the bands, say r4c1 < 8 instead

**Chris, 2026-09-22:** the 51..56 and 58+ clues will move so that both their
window top-left digits (8 and 9) see r4c1. To speed the search, drop the two
band clues and require only that the digit in r4c1 is at most 7.

**Runs.** Clues: QR 10 at the r6c6 window, QQRR 33 at r5c1, QQRR 5 in the
corner, r4c1 ≤ 7, no entered digits, no marks; the 7-digit seeing tie required.

1. One big solve per corner (`.scratch/place/place_probe.py`, 6 workers,
   1800 s): tl and bl paused for box load, tr and br killed at 14 minutes with
   nothing found. No verdict from this shape.
2. **Pair sweep** (`.scratch/place/pair_sweep.py`): one solve per unordered
   interior seeing pair (344 per corner), that pair's tie forced, 1 CP-SAT
   worker each, 6 in flight per corner, 180 s per pair, column 1 and 2 pairs
   first, the first br grid above as a solution hint. Every pair ends as a
   proven infeasible, a timeout (no verdict), or a hit.

**First hit, bottom-left corner**, pair r7c2 = r7c5 after 159.9 s, re-ranked
by `oracle.py`:

`591236748/642178935/378945612/764351829/283794561/159682473/436819257/915427386/827563194`

- QR 10 at r6c6; QQRR 33 at r5c1, 5 at r9c1; r4c1 = 7.
- Tie **r7c2 = r7c5**, same row: `4|35|25|17` and `43|52|51|7`, both 4352517,
  both QQRR 45. The single-digit rank sits first in r7c2 and last in r7c5.
- Where the dropped bands could go: windows with top-left 8 that see r4c1 are
  r4c7 (rank 53, does not see) — no; r5c2 (rank 54, same box as r4c1) — yes,
  inside 51..56. Windows with top-left 9 that see r4c1: r8c1 (rank 59, same
  column), inside 58+; r6c3 (rank 64, same box), also inside 58+. So this grid
  supports 51..56 at the r5c2 window and 58+ at r8c1 or r6c3.

It is the first preset in `docs/research/2026-09-22-qqrr-explorer.html`,
which now takes a per-preset clue map so only the QR 10 is drawn as a clue.
The sweep's per-corner tallies (proven pairs, timed-out pairs, hits) are
appended here when it finishes.
