# Codex adversarial-review trial (#812): rows for this repo

The controller runs a Codex adversarial pass on every heavy Claude-lane PR at merge time (implement/SKILL.md § The merge, step 3) and records one row per PR here: counts per class of what Codex found against Claude's own round-1 findings, and one line per codex-only confirmed finding. The trial's other repos keep their own copies of this table.

| Ticket | PR | codex-only confirmed | also found by Claude | disputed | Notes |
|---|---|---|---|---|---|
| #598 | #599 | 0 | 0 | 0 | Verdict approve, no findings, at both phases (early at 4fb3c5e refused as stale after the C1 fix commit; gate-retry at d73c4aa collected). Claude round 1: 5 findings, 4 fixed, 1 disputed. |
| #593 | #600 | 0 | 0 | 0 | Verdict approve, no findings. No early phase: "Round 1 out" never reached the controller, so the one run launched at the gate (phase recorded as gate-retry, 2f3838c, collected). Codex independently re-verified all ten grids against sudoku, rank tables and clues. Claude: round 1 (pre-#598) 15 findings all fixed then superseded; round 2 10 findings all fixed. |
| #601 | #602 | 0 | 0 | 0 | Verdict approve, no findings, at gate-retry (743df15, collected). Early pass at b8bd6ce read the branch mid-build and returned needs-attention with one [high] finding, "verdict note not yet written" — the pending deliverable, not a defect; refused as stale at the gate. Claude round 1: 10 findings, all fixed. |
| #603 | #604 | 0 | 0 | 1 | Verdict needs-attention with one [high] at gate (cff36af, collected): "sync_presets.py assumes the wrong HIT record layout, the q34 witness line precedes the tie". Disputed: the witness is written before the HIT marker, so the offsets hold, and the script synced all 48 grids of the 34–36 hunt without error. The worker, while checking, found a real adjacent gap Codex did not: a hit with two tie lines would shift the offset; noted on leftover #605. Claude round 1: 14 findings, 10 fixed, 3 leftover to #605, 1 confirmed. |
| #605 | #607 | 0 | 1 | 1 | Verdict needs-attention with one [medium] at gate (50865f0, collected): parse_hits admits a truncated HIT block and sync_presets crashes on it. Also found by Claude: round-1 C1 had filed it as #606; fixed adjacent at 8277655 and #606 closed. Second pass (8277655, collected): one [medium], malformed tie or grid line accepted as complete; controller ruled leftover, the partial-line half disputed (line-buffered writes) and the anchored-match hardening carried to the sweep. Claude round 1: 13 findings, 8 fixed, 1 filed, 3 leftover, 1 disputed. |
| #457 | #609 | 0 | 0 | 0 | Verdict approve, no findings, at gate (e17b712, collected). Claude round 1: 12 findings, 6 fixed adjacent, 6 disputed, 0 leftover. |
