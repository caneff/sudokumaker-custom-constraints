node_modules/
__pycache__/
*.pyc
# local scratch: puzzle solution dumps for the harness. gen.json is NOT
# scratch -- docs/example-layout.md makes it every example's shipped board.
fresh_sol.json
.venv/

# local session state and worktrees
.claude/
examples/skyscraper/.scan/

# code-review sub-agent report fallback; must never block git worktree remove
.scratch/

# #328 finder scratch
proto/find_*.log
